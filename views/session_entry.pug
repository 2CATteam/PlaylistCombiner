doctype html
html
	head
		title= session.name + " - Spotify Guessing Game"
		meta(charset="utf-8")
		meta(name="description", content="A game where you guess who listens to what Spotify songs")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", rel="stylesheet", integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH", crossorigin="anonymous")
		link(rel="stylesheet", type="text/css" href="/styles.css")
	body 
		.d-flex.justify-content-center.bg-success.p-5.min-vh-100
			.d-flex.w-100.justify-content-center.align-items-center.p-5#wrapContainer.bg-light.rounded-5
				.p-3
					h3.text-center= session.name 
					.d-flex.w-100.justify-content-center.flex-column.align-items-center
						p.wrap-text Welcome! You've been invited to play a game of song guessing!
						p To begin, log in with Spotify below:
						a.btn.btn-primary(href="/" + session.id + "/login") Log in here!
					hr
					.d-flex.justify-content-center
						p.d-none.text-success Successfully logged in!
					input#playlistOption(type="radio", name="inputType", value="playlist", disabled)
					label.ms-2(for="playlistOption") Playlist 
					.ms-3
						input#playlistLink.form-control(type="text", name="playlistLink")
						p.form-text= "Your session is set to accept " + session.size + " songs per person."
						.ms-3 
							p.form-text= "If your playlist is longer than this, only the first " + session.size + " songs will be included"
							p.form-text= "If your playlist is shorter than this, you will have fewer songs than others"
					input#topSongsOption(type="radio", name="inputType", value="topSongs", disabled)
					label.ms-2(for="topSongsOption") Top Songs
					.ms-3
						label.form-label(for="topSongsTimeRange") Time range for top songs:
						select#topSongsTimeRange.form-control(disabled)
							option(value="short_term") Short Term (~4 weeks)
							option(value="medium_term") Medium Term (~6 months)
							option(value="long_term") Long Term (1+ years)
					button.btn.btn-primary(disabled) Submit your songs!
		script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js", integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz", crossorigin="anonymous") 